// The values that make ttyl ttyl — each a plain promise, one line each.
export const values = [
  "Free & open source",
  "Local by design",
  "No account, no sign-in",
  "Zero telemetry",
] as const;
